# ffq distributed coordinator endpoint
# Used when building/running ffq-client with feature `distributed`.
FFQ_COORDINATOR_ENDPOINT=http://127.0.0.1:50051

# optional catalog file location (.json or .toml).
# defaults to ./ffq_tables/tables.json
FFQ_CATALOG_PATH=./ffq_tables/tables.json
